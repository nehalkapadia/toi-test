const fs = require('fs');
// const AWS = require('aws-sdk');
const { BUCKET_ENDPOINT, DO_SPACES_NAME, SPACES_SECRET, SPACES_KEY, CDN_URL } = process.env;

// Upload image into azure dev studio
exports.uploadImage = async (fileData, keyPath) => {};
